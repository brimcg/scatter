Copyright Brian McGinnis 2021

Scatter plot web app will plot CSV (comma-separated values) data shown in text area.  Data is editable and can contain multiple comma-separated columns of Y data.

Type or paste your data in the text area and update.  First row can be an optional text header describing the columns.
